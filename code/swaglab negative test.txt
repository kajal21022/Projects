package test.swaglab;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.Test;

public class negative_swaglabtest {
	//globally function(driver) declaration
		ChromeDriver driver;
		
		@Test(priority=1)
		void login()
		{
			driver= new ChromeDriver();
			driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(5));
			driver.get("https://www.saucedemo.com/");
			driver.manage().window().maximize();
			driver.findElement(By.name("user-name")).sendKeys("locked_out_user");
			driver.findElement(By.name("password")).sendKeys("133rfd");
			System.out.println("Enter User Details");
			driver.findElement(By.name("login-button")).click();
		}
}
